export const colors = {
  white: "#FFF",
  black: "#000",
};

const size = {
  mobile: "550px",
};

export const device = {
  mobile: `(min-width: ${size.mobile})`,
};

const theme = {
  colors,
  device
};

export default theme;
